# Проектная работа 11-го спринта

[Макет](<https://www.figma.com/file/vIywAvqfkOIRWGOkfOnReY/React-Fullstack_-Проектные-задачи-(3-месяца)_external_link?type=design&node-id=0-1&mode=design>)

[Чеклист](https://www.notion.so/praktikum/0527c10b723d4873aa75686bad54b32e?pvs=4)

## Выполненная работа:

### 1. Настройка окружения
- Создан файл `.env.local` с переменной `BURGER_API_URL` для хранения URL API

### 2. Настройка Redux store
- Создан глобальный Redux store с использованием Redux Toolkit
- Настроены слайсы для управления состоянием ингредиентов, конструктора бургера, пользователя и ленты заказов
- Реализована типизация для RootState и AppDispatch

### 3. Загрузка ингредиентов
- Реализована асинхронная загрузка ингредиентов с сервера через API
- Интеграция данных ингредиентов в компоненты через Redux store
- Настроены селекторы для получения ингредиентов в компонентах

### 4. Роутинг
- Настроен роутинг приложения с использованием React Router
- Созданы маршруты для всех страниц приложения
- Реализованы защищенные маршруты для страниц профиля

### 5. Функционал конструктора бургера
- Реализовано добавление ингредиентов в конструктор бургера
- Добавлена возможность перемещения ингредиентов в конструкторе
- Реализовано удаление ингредиентов из конструктора
- Настроено отображение счетчиков ингредиентов
- Реализована логика оформления заказа через API

### 6. Авторизация
- Настроена авторизация пользователя через API
- Реализованы формы регистрации, входа, восстановления и сброса пароля
- Добавлены защищенные маршруты, доступные только авторизованным пользователям
- Реализовано автоматическое обновление токена доступа

### 7. Лента заказов
- Реализована загрузка и отображение ленты заказов
- Настроено отображение заказов пользователя в профиле
- Интегрирована лента заказов с Redux store

## Важно:

Для корректной работы запросов к серверу необходимо добавить переменную BURGER_API_URL в окружение. Сама ссылка находится в файле `.env.example`.

## Новые изменения:

### Изменения от 27.02.2026 (актуально)
- В проекте устранены `any` в `src` (типизация ужесточена в `burger-constructor`, `feed-info`, `authSlice`).
- Оформление заказа доступно только авторизованным пользователям; для неавторизованных добавлен редирект на `/login`.
- После успешного оформления заказа корректно отображается модальное окно с номером заказа.
- Проверки: `npm run lint` и `npx tsc --noEmit` проходят без ошибок.

- Добавлены новые компоненты для отображения заказов
- Реализована функциональность отслеживания статуса заказа в реальном времени
- Улучшена система авторизации с автоматическим обновлением токенов
- Оптимизирована производительность при работе с большими списками ингредиентов

### Изменения от 24.02.2026
- Настроен роутинг по требованиям ПР11 (все маршруты и защищенные страницы).
- Добавлена защита для гостевых страниц через `ProtectedRoute` с флагом `onlyUnAuth`.
- Подключен `Provider` для Redux и исправлен запуск приложения без ошибок.
- Добавлен `rootReducer` и подключен к `store`.
- Создан слайс `orderInfo` с thunk для получения заказа по номеру.
- Компоненты `OrderInfo` и `IngredientDetails` получают данные из store с лоадерами.

### Изменения от 25.02.2026
- Конструктор очищается только после успешного создания заказа, закрытие модалки не сбрасывает состояние.
- Запрос ингредиентов выполняется один раз при старте приложения (в `App`), дублирующие вызовы удалены.
- Модальные окна открываются поверх текущей страницы через `background`-роутинг, без полной перерисовки.
- Для прямых ссылок на заказ добавлена загрузка данных по номеру и подключен `orderInfo` в store.
- До завершения `checkAuth` защищенные роуты показывают `Preloader`, чтобы не мелькал логин.
- Иконки в шапке меняют цвет в зависимости от активного маршрута.
- Вкладки ингредиентов (Булки/Соусы/Начинки) выровнены по центру.

### Основные изменения в проекте:
1. Настройка Redux store с различными слайсами (ingredients, burgerConstructor, user, feed, orderInfo)
2. Создание rootReducer для объединения всех редьюсеров
3. Реализация функционала заказа с созданием orderInfoSlice
4. Настройка роутинга с использованием React Router
5. Создание защищенных маршрутов с помощью ProtectedRoute
6. Компоненты для отображения информации о заказах и ингредиентах получают данные из Redux store
7. Добавлены модальные окна для отображения деталей заказов и ингредиентов

### Последние изменения:
- Обновлен компонент ProtectedRoute с поддержкой флага `unAuthOnly` для гостевых страниц
- Исправлен роутинг для страниц профиля и ленты заказов
- Добавлена загрузка данных ингредиентов в компоненте IngredientDetails
- Улучшена обработка ошибок при получении данных заказа
- Оптимизирована работа с селекторами Redux в компонентах OrderInfo и IngredientDetails
- Исправлены проблемы с отображением модальных окон для заказов и ингредиентов

### Изменения от 27.02.2026
- Полностью устранены `any` в исходниках TypeScript (`src`):
  - `src/components/ui/burger-constructor/type.ts` — `constructorItems` теперь строго типизирован через `TConstructorState['constructorItems']`.
  - `src/components/ui/feed-info/type.ts` — `feed` теперь типизирован как `Pick<TOrdersData, 'total' | 'totalToday'>`.
  - `src/slices/authSlice.ts` — в `catch` заменён `any` на `unknown`, добавлен безопасный helper `getErrorMessage`.
- Оформление заказа доступно только авторизованному пользователю:
  - В `src/components/burger-constructor/burger-constructor.tsx` добавлена проверка `selectIsAuthenticated`.
  - При нажатии кнопки оформления неавторизованным пользователем происходит редирект на `/login` с сохранением `from`.
- Исправлено отображение модального окна после успешного оформления заказа:
  - В `src/components/burger-constructor/burger-constructor.tsx` обновлён порядок действий: сначала `resetConstructor()`, затем `setOrderModalData(response.order)`, чтобы данные заказа не сбрасывались до открытия модалки.
- Обновлён Storybook-сценарий `src/stories/FeedInfo.stories.ts` под новую строгую типизацию `feed`.
- Проведена проверка проекта после изменений:
  - `npm run lint` — без ошибок.
  - `npx tsc --noEmit` — без ошибок.
  - Поиск `any` в `src` — совпадений не найдено.
### Изменения от 01.03.2026
- Обновлена ветка main до последней версии
- Создана новая ветка review на основе обновленной ветки main
- Установлен Cypress для e2e тестирования
- Добавлены скрипты для запуска Cypress тестов
- Создан файл с тестами для конструктора бургера (constructor.cy.tsx)
- Установлен Jest для модульного тестирования
- Добавлены скрипты для запуска Jest тестов
- Созданы тесты для всех слайсов Redux (ingredients, burgerConstructor, auth, user, feed, profileOrders, orderInfo)
- Удалены старые ветки и созданы новые для дальнейшей разработки
  - Dev-сервер запускается на `http://localhost:4001`.
